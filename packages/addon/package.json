{
  "name": "@easynews-plus-plus/addon",
  "version": "1.0.1",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest watch",
    "build": "tsc",
    "prepublish": "npm run build",
    "start": "node dist/server.js",
    "start:dev": "cross-env NODE_ENV=dev tsx watch src/server.ts"
  },
  "description": "Provides content from Easynews & includes a search catalog. This addon can also be self-hosted. It uses a different auth implementation than other Easynews addons and has other enhancements that works on multiple plattforms. Check out the Github repo for more information.",
  "devDependencies": {
    "@types/stremio-addon-sdk": "^1.6.11"
  },
  "dependencies": {
    "@easynews-plus-plus/api": "^1.0.0",
    "parse-torrent-title": "^1.4.0",
    "stremio-addon-sdk": "^1.6.10"
  }
}
